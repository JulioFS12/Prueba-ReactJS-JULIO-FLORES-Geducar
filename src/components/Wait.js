import React from 'react';
import { wait } from '../assets/images';

export const Wait = () => {
   return(
       <section className="wait-container">
            <img src={`${wait}`}/>
       </section>
   )
}